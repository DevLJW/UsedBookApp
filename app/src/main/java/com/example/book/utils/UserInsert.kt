/*
package com.example.book.utils

import com.google.firebase.database.ktx.database
import com.google.firebase.ktx.Firebase

class UserInsert {

    companion object {

        private val database = Firebase.database

        var PassWord = database.getReference("UserInfo") //부모경로를 지정하여 PassWord 변수에저장
        var Email = database.getReference("UserInfo") //부모경로를 지정하여 Email 변수에저장
        var PhoneNumber = database.getReference("UserInfo")  //부모경로를 지정하여 phonenumber 변수에저장
        var Address = database.getReference("UserInfo")      //부모경로를 지정하여 Address 변수에저장
        var UserKey = database.getReference("UserInfo")      //부모경로를 지정하여 Userkey 변수에저장
    }
}

 */